spring:
  profiles: eureka1
  application:
    name: spring-cloud-eureka
server:
  port: 8089

eureka:
  instance:
    homename: eureka1
  client:
    # 是否将自己作为服务注册
    register-with-eureka: true
    # 
    fetch-registry: true
    serviceUrl:
      defaultZone: http://eureka2:8080/eureka/

